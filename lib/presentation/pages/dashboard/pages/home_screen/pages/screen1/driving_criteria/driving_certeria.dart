import 'package:flutter/material.dart';

class DrivingCerteria extends StatelessWidget {
  const DrivingCerteria({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Driving License Critria"),
      ),
      body: Column(
        children: [
          

        ],
      ),
    );
  }
}
