import 'package:flutter/material.dart';

class RtoOffice extends StatelessWidget {
  const RtoOffice({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("RTO OFFICE"),
      ),
      body: Column(
        children: [Text("rto office")],
      ),
    );
  }
}
