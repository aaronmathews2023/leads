import 'package:flutter/material.dart';

class FromsR extends StatelessWidget {
  const FromsR({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Forms"),
      ),
      body: Center(
        child: Column(
          children: [Text("forms")],
        ),
      ),
    );
  }
}
